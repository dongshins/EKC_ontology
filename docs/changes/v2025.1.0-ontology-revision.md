# EKC 2025 [v2025.1.0] — Ontology revision dossier (linking/mapping + metadata policy)

- Evidence basis = TTL diff between `ekc_2025_0_0.ttl` and `ekc_2025_1_0.ttl`
- Ontology IRI (base) = `http://dh.aks.ac.kr/ontologies/ekc`
- Version IRI (2025) = `http://dh.aks.ac.kr/ontologies/ekc-2025`
- Scope = header normalization + linking/mapping guidance + metadata-property policy (NOT class-level refactoring)

---

## 0. Purpose
EKC 2025 [v2025.1.0]은 EKC 2025 [v2025.0.0]의 스키마 골격을 유지하면서,
(1) 발행/수정 메타데이터를 정규화하고,
(2) 동일성 링크와 외부 어휘 매핑의 구분 원칙을 명문화하며,
(3) `dc:*`와 `dcterms:*` 메타데이터 프로퍼티의 사용 정책을 정리한 패치 릴리즈이다. @ko

---

## 1. Diff at a glance

### Changed
- `owl:versionInfo` updated from `EKC 2025` to `EKC 2025 [v2025.1.0]`
- `dcterms:issued` normalized
- `dcterms:modified` updated to patch date
- `owl:priorVersion` normalized for consistent IRI form
- Added `dc:publisher` (EN/KO literals)
- Added `dcat:contactPoint`
- Added/curated policy descriptions for `dc:creator`, `dc:contributor`, `dc:publisher`
- Added/curated `owl:sameAs`, `skos:mappingRelation`, `skos:exactMatch`, `skos:closeMatch`
- Removed ontology-header literal list in `dcterms:contributor`

### Not changed
- Class set (owl:Class): no change
- Datatype properties: no change
- Core EKC schema architecture: no refactoring

---

## 2. Ontology header changes

### 2.1 Version information
- BEFORE: `owl:versionInfo "EKC 2025"@en`
- AFTER: `owl:versionInfo "EKC 2025 [v2025.1.0]"@en`

### 2.2 Issued / Modified
- `dcterms:issued` was normalized
- `dcterms:modified` was updated to the v2025.1.0 patch date

### 2.3 Prior version IRI
- `owl:priorVersion` was normalized to a consistent IRI form

### 2.4 Publisher / contact point
- Added `dc:publisher` with EN/KO literals
- Added `dcat:contactPoint` for repository-level contact reference

### 2.5 Contributor header literal
- The ontology-header literal list in `dcterms:contributor` was removed
- Contributor presentation is maintained in repository documentation instead

---

## 3. Metadata-property policy (`dc:*` vs `dcterms:*`)

### 3.1 Policy rationale
- Use `dcterms:creator`, `dcterms:contributor`, `dcterms:publisher` when the value is a **resource**
- Use `dc:creator`, `dc:contributor`, `dc:publisher` when the value is **literal/annotation-style text or IRI text**

### 3.2 Practical effect
This distinction improves consistency between ontology semantics and repository/publication metadata, especially in annotation-heavy documentation workflows. @en

---

## 4. Identity vs mapping

### 4.1 `owl:sameAs`
- Reserved for strict identity between two URIs denoting the same individual/resource
- Not for loose similarity, topical relatedness, or vocabulary alignment

### 4.2 `skos:mappingRelation`
- Introduced/curated as a super-property for external conceptual mapping

### 4.3 `skos:exactMatch`
- For close conceptual equivalence across vocabularies
- Distinct from OWL identity semantics

### 4.4 `skos:closeMatch`
- For near correspondence across vocabularies
- Not a substitute for `owl:sameAs`

---

## 5. Compatibility / migration notes
- No class-set migration required
- No datatype-property migration required
- Review any tooling that relied on ontology-header contributor literals
- Review any rule depending on exact `http`/`https` matching in version lineage IRIs

---

## 6. Release positioning
v2025.1.0 should be understood as a **policy-and-metadata stabilization patch** rather than a schema redesign. @en

---

## 7. Evidence
- Compare: `v2025.0.0...v2025.1.0`
- Release: `v2025.1.0`
- Basis: direct TTL comparison of `ekc_2025_0_0.ttl` and `ekc_2025_1_0.ttl`

---

## 8. Versioning metadata policy

The following note clarifies how ontology-level and release-level versioning are distinguished in the present repository structure.

During the final review of `v2025.1.0`, the relationship between `dcterms:replaces` and `owl:priorVersion` was reconsidered in order to determine whether the ontology header should be revised again and republished as `ekc_2025_1_1.ttl`.

The conclusion was to retain the current metadata structure.

In the present repository policy, `http://dh.aks.ac.kr/ontologies/ekc-2025` is treated as the ontology-level version IRI for the EKC 2025 line, while `v2025.1.0` serves as the release-level designation expressed in `owl:versionInfo`, release metadata, and related repository documentation.

Accordingly:

- `dcterms:replaces` continues to indicate that the current EKC 2025 ontology line supersedes `ekc-2022-alpha2507`;
- `owl:priorVersion` continues to identify `ekc-2022-alpha2507` as the ontology-level predecessor of the current `ekc-2025` line;
- `v2025.0.0` and `v2025.1.0` are not modeled here as separate ontology version IRIs linked by `owl:priorVersion`.

For this reason, no additional ontology file such as `ekc_2025_1_1.ttl` is issued solely to reinterpret these two properties.

This decision should be read as a clarification of repository versioning policy, not as a correction of a defective ontology header. Under the present model, the existing values remain appropriate because the EKC 2025 ontology line is being maintained as a single ontology-level version line with release-level refinements documented through `owl:versionInfo`, GitHub release records, and accompanying change documentation.

This policy also provides the basis for the wording adopted in the `README.md`, `CHANGELOG.md`, and release note associated with `v2025.1.0`.
